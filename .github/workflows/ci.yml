name: CI

on:
  pull_request: {}

permissions:
  statuses: write
  checks: write
  contents: read
  pull-requests: read

jobs:
  pr-checks:
    uses: actionsforge/actions/.github/workflows/markdown-pr-checks.yml@main
